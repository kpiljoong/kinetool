# A simple tool to interact with Amazon Kinesis services.

## Commands

* ls: list of streams
* lss: list of shards in the given stream
* put-dummy: generates dummy and sends to a stream
* tail: tails the given stream